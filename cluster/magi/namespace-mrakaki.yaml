apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ns-mrakaki
  namespace: flux-system
spec:
  interval: 10m
  path: ./namespaces/magi/mrakaki
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  dependsOn:
    - name: ns-databases
  decryption:
    provider: sops
    secretRef:
      name: sops-age