apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: ns-home
  namespace: flux-system
spec:
  interval: 10m
  path: ./namespaces/magi/home
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  dependsOn:
    - name: ns-databases
  decryption:
    provider: sops
    secretRef:
      name: sops-age