apiVersion: v1
kind: Service
metadata:
  name: immich
  labels:
    app: immich
spec:
  ports:
  - port: 2283
    name: http
  type: LoadBalancer
  selector:
    app: immich
---
apiVersion: v1
kind: Service
metadata:
  name: immich-machine-learning
  labels:
    app: immich-ml
spec:
  ports:
  - port: 3003
    name: http
  type: ClusterIP
  selector:
    app: immich-ml